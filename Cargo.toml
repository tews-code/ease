[package]
name = "ease"
version = "0.1.0"
edition = "2024"
description = "EASE - OS for RP2350"

[lib]
name = "ease"
path = "src/lib.rs"

[[bin]]
name = "ease"
path = "src/main.rs"

[features]
default = ["test-all", "alloc-freelist"]
qemu = []  # For QEMU-specific code
alloc-bump = []
alloc-freelist = []
alloc-slab = []
test-alloc = []
test-bss = []
test-clint = []
test-collections = []
test-fs = []
test-io = []
test-shell = []
test-stack-guard = []
test-timer = []
test-virtio = []
test-all = ["test-alloc", "test-bss", "test-clint", "test-collections", "test-fs", "test-io", "test-shell", "test-stack-guard", "test-timer", "test-virtio"]

[profile.test]
opt-level = 1

[package.metadata.docs.rs]
targets = ["riscv32imac-unknown-none-elf"]
